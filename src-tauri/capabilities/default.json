{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the cashier window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
